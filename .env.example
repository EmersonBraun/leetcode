# GitHub Pages deployment (required for yarn deploy)
GIT_USER=your_github_username
# Personal Access Token (generate at https://github.com/settings/tokens)
# Requires 'repo' scope. Do NOT commit this value.
GIT_PASS=your_personal_access_token

# Algolia DocSearch (required for yarn start / yarn build)
# See README → Search (Algolia) for where to get these values.
ALGOLIA_APP_ID=your_application_id
ALGOLIA_API_KEY=your_search_only_api_key
